package main

func isValid(s string) bool {
	stack := []rune{}
	data := map[rune]rune{')': '(', '}': '{', ']': '['}

	for _, char := range s {
		if char == ''
	}

	// for key, value := range data {

	// }

}
